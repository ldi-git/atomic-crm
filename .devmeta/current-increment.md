# Current Increment

**Active:** Increment 2-wpr — Pipeline Stage Win Probability: each Deals Kanban column header shows a weighted pipeline total (sum of `amount × probability`) next to the existing raw amount sum. Pure frontend, no schema changes. Status: NOT STARTED.

## History

- Increment 1-kpv — Supabase Seed Data: `make seed` resets the local Supabase DB and loads a populated, funnel-shaped CRM with `demo@atomic.dev` / `demodemo123` login. Status: NOT STARTED.
